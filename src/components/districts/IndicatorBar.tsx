export default function IndicatorBar({ label, value }: { label: string; value: number }) { return <div className="indicator"><div className="indicator-label"><span>{label}</span><span>{value < 40 && <span className="critical-badge">Critical</span>}<strong>{value}</strong><span className="muted"> / 100</span></span></div><div className={`indicator-track ${value < 40 ? "critical" : ""}`} role="meter" aria-label={label} aria-valuemin={0} aria-valuemax={100} aria-valuenow={value}><span style={{width: `${Math.max(0, Math.min(100, value))}%`}}/></div></div>; }
